{
  "name": "typescript-simple-template",
  "version": "1.0.0",
  "main": "dist/main.js",
  "private": true,
  "scripts": {
    "postinstall": "bun x cre-setup",
    "simulate": "cre workflow simulate . --target staging-settings",
    "simulate-broadcast": "cre workflow simulate . --target staging-settings --broadcast",
    "deploy:staging": "cre workflow deploy . --target staging-settings",
    "deploy:production": "cre workflow deploy . --target production-settings"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.9",
    "viem": "^2.47.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "1.2.21"
  }
}
